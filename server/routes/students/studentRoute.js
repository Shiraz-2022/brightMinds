const express = require('express');
const router = express.Router();
const studentController = require('../../controller/students/studentController');


router.post('/report', parentController.studentController);

module.exports = router;